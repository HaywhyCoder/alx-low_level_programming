#include "main.h"

/**
 *_abs - compute the absolute of an integer
 *@n: The number to be checked
 *
 *Return: absolute of the integer
 */

int _abs(int n)
{
	if (n >= 0)
		return (n);
	else
		return (-n);
}

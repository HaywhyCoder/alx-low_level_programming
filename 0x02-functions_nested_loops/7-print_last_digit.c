#include "main.h"

/**
 *print_last_digit - prints the last digit of a number
 *@n: number to be checked
 *
 *Return: last digit of the number inputed
 */

int print_last_digit(int n)
{
	last_digit = n % 10;
	return (last_digit);
}
